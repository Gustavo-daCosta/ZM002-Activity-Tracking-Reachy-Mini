# Reachy Mini daemon REST API — endpoint reference

Generated by `tools/gen_api_reference.py` from `http://192.168.137.171:8000/openapi.json` (daemon 1.10.0).
Base URL: `http://<host>:8000`. 🔴 = ask the user before calling. Example responses are live captures.

## Index

- [Daemon](#daemon) — 9 endpoints
- [State](#state) — 5 endpoints
- [Move](#move) — 8 endpoints
- [Motors](#motors) — 2 endpoints
- [Media, tracking & sounds](#media-tracking--sounds) — 14 endpoints
- [Camera](#camera) — 1 endpoints
- [Audio & volume](#audio--volume) — 7 endpoints
- [Apps](#apps) — 15 endpoints
- [Kinematics](#kinematics) — 3 endpoints
- [Hugging Face auth](#hugging-face-auth) — 15 endpoints
- [Wi-Fi](#wi-fi) — 10 endpoints
- [Update](#update) — 6 endpoints
- [Cache](#cache) — 2 endpoints
- [Web pages (redirects)](#web-pages-redirects) — 3 endpoints

## Daemon

### `POST /api/daemon/start` 🔴

**Start Daemon** — Start the daemon.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `wake_up` | query | yes | boolean |  |  |

**Response 200**: object[str → string]

### `POST /api/daemon/stop` 🔴

**Stop Daemon** — Stop the daemon, optionally putting the robot to sleep.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `goto_sleep` | query | yes | boolean |  |  |

**Response 200**: object[str → string]

### `POST /api/daemon/restart` 🔴

**Restart Daemon** — Restart the daemon.

**Response 200**: object[str → string]

### `GET /api/daemon/status`

**Get Daemon Status** — Get the current status of the daemon.

**Response 200**: `DaemonStatus`

Example (`GET /api/daemon/status`):

```json
{
  "type": "daemon_status",
  "robot_name": "reachy_mini",
  "state": "running",
  "wireless_version": true,
  "desktop_app_daemon": false,
  "simulation_enabled": false,
  "mockup_sim_enabled": false,
  "no_media": false,
  "media_released": false,
  "camera_specs_name": "wireless",
  "backend_status": {
    "ready": true,
    "motor_control_mode": "enabled",
    "last_alive": 1789126759.573334,
    "control_loop_stats": {
      "mean_control_loop_frequency": 49.62197588851465,
      "max_control_loop_interval": 0.02019023895263672,
      "nb_error": 0,
      "motor_controller": "ControlLoopStats(period=~20.00ms, read_dt=~1.88 ms, write_dt=~0.06 ms)"
    },
    "error": null
  },
  "error": null,
  "wlan_ip": "192.168.137.171",
  "version": "1.10.0",
  "hardware_id": "57a88605e96ae556",
  "face_target": {
    "detected": false,
    "x": null,
    "y": null,
    "roll": null,
    "ts": null
  }
}
```

### `GET /api/daemon/robot-name`

**Get Robot Display Name** — Return the robot's current display name. Reports the effective/advertised name: the persisted override when a client has renamed the robot, otherwise the ``--robot-name`` default the daemon is running with. A client can pre-fill a rename field from this without having to fall back to the daemon status for the default.

**Response 200**: `RobotNameResponse`

Example (`GET /api/daemon/robot-name`):

```json
{
  "name": "reachy_mini"
}
```

### `POST /api/daemon/robot-name` 🔴

**Set Robot Display Name** — Persist a new robot display name and apply it live (no restart). Persists the name to disk, then refreshes the advertised copies so the rename takes effect immediately: the daemon status + central relay (via ``Daemon.apply_robot_name``) and the LAN mDNS record. This is the REST entry point used by the BLE setup service; the WebRTC ``set_robot_name`` command goes through the same live-apply path on the backend.

**Body** (required): application/json: `RobotNameRequest`

**Response 200**: `RobotNameResponse`

### `GET /api/daemon/hardware-id`

**Get Robot Hardware Id** — Robot-unique hardware ID — the Pollen audio device's USB serial. Returns ``{"hardware_id": "<serial>"}`` (or ``null`` when no robot is attached, e.g. on a developer machine). Same value across Lite and Wireless variants; same value across reboots and OS reinstalls.

**Response 200**: object[str → string \| null]

Example (`GET /api/daemon/hardware-id`):

```json
{
  "hardware_id": "57a88605e96ae556"
}
```

### `GET /api/daemon/robot-app-lock-status`

**Get Robot App Lock Status** — Return the current state of the robot's managed-app lock. The daemon's single source of truth for which managed app (if any) currently holds the robot: - ``free``: no managed app holds the slot. - ``local_app``: a Python app launched via AppManager is running. ``holder_name`` is the app name. - ``remote_session``: a remote WebRTC client is connected via the central signaling relay. ``holder_name`` is a generic ``"remote"`` placeholder (the real consumer app name lives on the central server and is surfaced via its own ``/api/robot-status``). Note that SDK clients talking to the daemon directly bypass this lock; it only reflects the two *managed* app entry points. Intended for UI layers (desktop app, dashboard) that want to render a busy/free indicator without trying to open a session.

**Response 200**: `RobotAppLockStatus`

Example (`GET /api/daemon/robot-app-lock-status`):

```json
{
  "state": "free",
  "holder_name": null
}
```

### `POST /health-check`

**Health Check** — Health check endpoint to reset the health check timer.

**Response 200**: object[str → string]

## State

### `GET /api/state/present_head_pose`

**Get Head Pose** — Get the present head pose. Arguments: use_pose_matrix (bool): Whether to use the pose matrix representation (4x4 flattened) or the translation + Euler angles representation (x, y, z, roll, pitch, yaw). backend (Backend): The backend instance. Returns: AnyPose: The present head pose.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `use_pose_matrix` | query | no | boolean | false |  |

**Response 200**: `XYZRPYPose` \| `Matrix4x4Pose`

Example (`GET /api/state/present_head_pose`):

```json
{
  "x": -0.021165637985569318,
  "y": 0.0017600638717936816,
  "z": -0.0439194070321931,
  "roll": 0.0032008237151059434,
  "pitch": 0.4441960589502889,
  "yaw": -0.02290193227732809
}
```

### `GET /api/state/present_body_yaw`

**Get Body Yaw** — Get the present body yaw (in radians).

**Response 200**: number

Example (`GET /api/state/present_body_yaw`):

```json
-0.010737865515199285
```

### `GET /api/state/present_antenna_joint_positions`

**Get Antenna Joint Positions** — Get the present antenna joint positions (in radians) - (left, right).

**Response 200**: array[any]

Example (`GET /api/state/present_antenna_joint_positions`):

```json
[
  -3.05108778710454,
  3.031146036862027
]
```

### `GET /api/state/doa`

**Get Doa** — Get the Direction of Arrival from the microphone array. Returns the angle in radians (0=left, π/2=front, π=right) and speech detection status. Returns None if the audio device is not available or no reading has landed yet (the first one lands within ~100 ms of the first request).

**Response 200**: `DoaSnapshot` \| null

Example (`GET /api/state/doa`):

```json
null
```

### `GET /api/state/full`

**Get Full State** — Get the full robot state, with optional fields.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `with_control_mode` | query | no | boolean | true |  |
| `with_head_pose` | query | no | boolean | true |  |
| `with_target_head_pose` | query | no | boolean | false |  |
| `with_head_joints` | query | no | boolean | false |  |
| `with_target_head_joints` | query | no | boolean | false |  |
| `with_body_yaw` | query | no | boolean | true |  |
| `with_target_body_yaw` | query | no | boolean | false |  |
| `with_antenna_positions` | query | no | boolean | true |  |
| `with_target_antenna_positions` | query | no | boolean | false |  |
| `with_passive_joints` | query | no | boolean | false |  |
| `with_doa` | query | no | boolean | false |  |
| `use_pose_matrix` | query | no | boolean | false |  |

**Response 200**: `FullState`

Example (`GET /api/state/full`):

```json
{
  "control_mode": "enabled",
  "head_pose": {
    "x": -0.021165637985569318,
    "y": 0.0017600638717936816,
    "z": -0.0439194070321931,
    "roll": 0.0032008237151059434,
    "pitch": 0.4441960589502889,
    "yaw": -0.02290193227732809
  },
  "head_joints": null,
  "body_yaw": -0.010737865515199285,
  "antennas_position": [
    -3.05108778710454,
    3.031146036862027
  ],
  "timestamp": "2026-09-11T11:39:19.652862Z",
  "passive_joints": null,
  "doa": null
}
```

## Move

### `GET /api/move/running`

**Get Running Moves** — Get a list of currently running move tasks.

**Response 200**: array[`MoveUUID`]

Example (`GET /api/move/running`):

```json
[]
```

### `POST /api/move/goto`

**Goto** — Request a movement to a specific target.

**Body** (required): application/json: `GotoModelRequest`

**Response 200**: `MoveUUID`

### `POST /api/move/play/wake_up`

**Play Wake Up** — Request the robot to wake up.

**Response 200**: `MoveUUID`

### `POST /api/move/play/goto_sleep`

**Play Goto Sleep** — Request the robot to go to sleep.

**Response 200**: `MoveUUID`

### `GET /api/move/recorded-move-datasets/list/{dataset_name}`

**List Recorded Move Dataset** — List available recorded moves in a dataset.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `dataset_name` | path | yes | string |  |  |

**Response 200**: array[string]

Example (`GET /api/move/recorded-move-datasets/list/pollen-robotics/reachy-mini-emotions-library`):

```json
[
  "understanding2",
  "scared1",
  "displeased1",
  "sad2",
  "curious1",
  "dance3",
  "waiting",
  "yes_sad1",
  "shy1",
  "resigned1",
  "relief1",
  "dying1",
  "go_away1",
  "attentive1",
  "exhausted1",
  "reprimand2",
  "come1",
  "surprised1",
  "attentive2",
  "indifferent1",
  "thoughtful1",
  "laughing1",
  "inquiring1",
  "fear1",
  "impatient1",
  "contempt1",
  "helpful2",
  "success1",
  "reprimand1",
  "uncomfortable1",
  "lost1",
  "dance1",
  "relief2",
  "frustrated1",
  "boredom1",
  "grateful1",
  "proud1",
  "inquiring3",
  "confused1",
  "irritated1",
  "welcoming2",
  "impatient2",
  "no_sad1",
  "no1",
  "cheerful1",
  "proud2",
  "inquiring2",
  "amazed1",
  "surprised2",
  "mini-deep-sleep",
  "disgusted1",
  "uncertain1",
  "welcoming1",
  "anxiety1",
  "sad1",
  "reprimand3",
  "irritated2",
  "oops2",
  "sleep1",
  "serenity1",
  "calming1",
  "electric1",
  "yes1",
  "loving1",
  "incomprehensible2",
  "understanding1",
  "enthusiastic1",
  "rage1",
  "success2",
  "tired1",
  "oops1",
  "toc-toc-toc",
  "wake-mini-up",
  "lonely1",
  "furious1",
  "thoughtful2",
  "proud3",
  "helpful1",
  "boredom2",
  "downcast1",
  "laughing2",
  "displeased2",
  "no_excited1",
  "dance2",
  "enthusiastic2"
]
```

### `POST /api/move/play/recorded-move-dataset/{dataset_name}/{move_name}`

**Play Recorded Move Dataset** — Request the robot to play a predefined recorded move from a dataset.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `dataset_name` | path | yes | string |  |  |
| `move_name` | path | yes | string |  |  |

**Response 200**: `MoveUUID`

### `POST /api/move/stop`

**Stop Move** — Stop a running move task.

**Body** (required): application/json: `MoveUUID`

**Response 200**: object[str → string]

### `POST /api/move/set_target`

**Set Target** — POST route to set a single FullBodyTarget.

**Body** (required): application/json: `FullBodyTarget`

**Response 200**: object[str → string]

## Motors

### `GET /api/motors/status`

**Get Motor Status** — Get the current status of the motors.

**Response 200**: `MotorStatus`

Example (`GET /api/motors/status`):

```json
{
  "mode": "enabled"
}
```

### `POST /api/motors/set_mode/{mode}`

**Set Motor Mode** — Set the motor control mode.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `mode` | path | yes | `MotorControlMode` |  |  |

**Response 200**: object[str → string]

## Media, tracking & sounds

### `POST /api/media/release`

**Release Media** — Release camera and audio hardware for direct client access.

**Response 200**: object[str → string]

### `POST /api/media/acquire`

**Acquire Media** — Re-acquire camera and audio hardware.

**Response 200**: object[str → string]

### `GET /api/media/status`

**Media Status** — Get the current media status.

**Response 200**: object[str → boolean]

Example (`GET /api/media/status`):

```json
{
  "available": true,
  "released": false,
  "no_media": false
}
```

### `POST /api/media/play_sound`

**Play Sound** — Play a sound file on the robot's speaker. The *file* field can be: - An absolute path on the daemon's filesystem. - A filename relative to the built-in assets directory. - A filename previously uploaded to the sounds temp directory.

**Body** (required): application/json: `PlaySoundRequest`

**Response 200**: object[str → string]

### `POST /api/media/stop_sound`

**Stop Sound** — Stop the currently playing sound file.

**Response 200**: object[str → string]

### `POST /api/media/clear_incoming_audio`

**Clear Incoming Audio** — Drop audio received from WebRTC clients that is queued for the speaker. Used for barge-in so the robot stops speaking already-buffered audio.

**Response 200**: object[str → string]

### `POST /api/media/wobbling/enable`

**Enable Wobbling** — Enable audio-reactive head wobbling. When enabled, audio played on the daemon (sounds, incoming WebRTC audio) is analysed and converted into subtle head movements.

**Response 200**: object[str → string]

### `POST /api/media/wobbling/disable`

**Disable Wobbling** — Disable audio-reactive head wobbling and reset offsets.

**Response 200**: object[str → string]

### `POST /api/media/tracking/enable`

**Enable Tracking** — Enable daemon-side visual head tracking.

**Body** (optional): application/json: `EnableTrackingRequest` \| null

**Response 200**: object[str → string \| boolean]

### `POST /api/media/tracking/disable`

**Disable Tracking** — Disable daemon-side visual head tracking.

**Response 200**: object[str → string \| boolean]

### `GET /api/media/tracking/face`

**Get Tracked Face** — Return the latest face observed by daemon-side head tracking.

**Response 200**: object

Example (`GET /api/media/tracking/face`):

```json
{
  "status": "ok",
  "face_target": {
    "detected": false,
    "x": null,
    "y": null,
    "roll": null,
    "ts": null
  }
}
```

### `POST /api/media/sounds/upload`

**Upload Sound** — Upload a sound file to the daemon's temporary sound directory. The file is saved to ``/tmp/reachy_mini_sounds/<original_filename>``. If a file with the same name already exists it is overwritten. The upload is restricted to known audio extensions, capped at ``MAX_SOUND_UPLOAD_BYTES``, and validated by content before being stored, so non-audio payloads cannot be written to disk. Returns: JSON with the absolute *path* of the saved file on the daemon.

**Body** (required): multipart/form-data: `Body_upload_sound_api_media_sounds_upload_post`

**Response 200**: object[str → string]

### `GET /api/media/sounds`

**List Sounds** — List sound files in the daemon's temporary sound directory.

**Response 200**: object[str → array[string]]

Example (`GET /api/media/sounds`):

```json
{
  "files": []
}
```

### `DELETE /api/media/sounds/{filename}`

**Delete Sound** — Delete a sound file from the daemon's temporary sound directory. Only files inside the temp directory can be deleted (no path traversal).

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `filename` | path | yes | string |  |  |

**Response 200**: object[str → string]

## Camera

### `GET /api/camera/specs`

**Get Camera Specs** — Get the detected camera specifications. Returns the camera name, available resolutions, default resolution, intrinsic matrix (K) and distortion coefficients (D) for the camera that the daemon detected at startup.

**Response 200**: `CameraSpecsResponse`

Example (`GET /api/camera/specs`):

```json
{
  "name": "wireless",
  "available_resolutions": [
    {
      "name": "R1280x720at30fps",
      "width": 1280,
      "height": 720,
      "fps": 30,
      "crop_factor": 1.0
    },
    {
      "name": "R1920x1080at30fps",
      "width": 1920,
      "height": 1080,
      "fps": 30,
      "crop_factor": 1.115
    },
    {
      "name": "R1280x720at60fps",
      "width": 1280,
      "height": 720,
      "fps": 60,
      "crop_factor": 1.0
    },
    {
      "name": "R3840x2592at10fps",
      "width": 3840,
      "height": 2592,
      "fps": 10,
      "crop_factor": 1.0
    },
    {
      "name": "R3840x2160at10fps",
      "width": 3840,
      "height": 2160,
      "fps": 10,
      "crop_factor": 1.109
    },
    {
      "name": "R3264x2448at10fps",
      "width": 3264,
      "height": 2448,
      "fps": 10,
      "crop_factor": 1.115
    },
    {
      "name": "R3072x1728at10fps",
      "width": 3072,
      "height": 1728,
      "fps": 10,
      "crop_factor": 1.0
    }
  ],
  "default_resolution": {
    "name": "R1280x720at30fps",
    "width": 1280,
    "height": 720,
    "fps": 30,
    "crop_factor": 1.0
  },
  "K": [
    [
      2001.8076426486707,
      0.0,
      1905.876059826701
    ],
    [
      0.0,
      2003.0778885944105,
      1328.3239717935594
    ],
    [
      0.0,
      0.0,
      1.0
    ]
  ],
  "D": [
    -1.4652320301298614,
    0.6542714131667414,
    0.012147809271745049,
    -0.002677286460143648,
    0.3035939941825349,
    -1.4300809080461876,
    
... (truncated)
```

## Audio & volume

### `POST /api/audio/config/apply` 🔴

**Apply Audio Config** — Write a batch of XVF3800 parameters and optionally verify them.

**Body** (required): application/json: `ApplyAudioConfigRequest`

**Response 200**: `ApplyAudioConfigResponse`

### `GET /api/audio/config/parameter/{name}`

**Read Audio Parameter** — Read a single XVF3800 parameter by name.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `name` | path | yes | string |  |  |

**Response 200**: `ReadAudioParameterResponse`

### `GET /api/volume/current`

**Get Volume** — Get the current output volume level.

**Response 200**: `VolumeResponse`

Example (`GET /api/volume/current`):

```json
{
  "volume": 62,
  "platform": "Linux",
  "device": "Reachy Mini Audio"
}
```

### `POST /api/volume/set`

**Set Volume** — Set the output volume level and play a test sound.

**Body** (required): application/json: `VolumeRequest`

**Response 200**: `VolumeResponse`

### `POST /api/volume/test-sound`

**Play Test Sound** — Play a test sound.

**Response 200**: `TestSoundResponse`

### `GET /api/volume/microphone/current`

**Get Microphone Volume** — Get the current microphone input volume level.

**Response 200**: `VolumeResponse`

Example (`GET /api/volume/microphone/current`):

```json
{
  "volume": 100,
  "platform": "Linux",
  "device": "Reachy Mini Audio"
}
```

### `POST /api/volume/microphone/set`

**Set Microphone Volume** — Set the microphone input volume level.

**Body** (required): application/json: `VolumeRequest`

**Response 200**: `VolumeResponse`

## Apps

### `GET /api/apps/list-available/{source_kind}`

**List Available Apps** — List available apps (including not installed).

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `source_kind` | path | yes | `SourceKind` |  |  |

**Response 200**: array[`AppInfo`]

Example (`GET /api/apps/list-available/installed`):

```json
[
  {
    "name": "reachy_mini_conversation_app",
    "source_kind": "installed",
    "description": "",
    "url": null,
    "extra": {
      "custom_app_url": "http://0.0.0.0:7860/",
      "venv_path": "/venvs/apps_venv",
      "id": "pollen-robotics/reachy_mini_conversation_app",
      "author": "pollen-robotics",
      "likes": 172,
      "downloads": 0,
      "createdAt": "2025-10-16T16:06:57.000Z",
      "lastModified": "2026-08-19T19:19:09.000Z",
      "runtime": {
        "stage": "RUNNING",
        "hardware": {
          "current": null,
          "requested": null
        },
        "replicas": {
          "requested": 1,
          "current": 1
        }
      },
      "tags": [
        "static",
        "reachy_mini",
        "reachy_mini_python_app",
        "region:us"
      ],
      "isPythonApp": true,
      "cardData": {
        "emoji": "\ud83c\udfa4",
        "short_description": "Talk with Reachy Mini!",
        "sdk": "static",
        "tags": [
          "reachy_mini",
          "reachy_mini_python_app"
        ],
        "title": "Reachy Mini Conversation App",
        "colorFrom": "red",
        "colorTo": "blue",
        "pinned": false,
        "suggested_storage": "large"
      },
      "sdk": "static",
      "private": false,
      "_id": "68f11821ccbf0a0c900e7b39",
      "sha": "ddc309630448a664b0283812ff80048c36966c35",
      "subdomain": "pollen-robotics-reachy-mini-conversation-app",
      "gated": false,
      "disabled": false,
      "host": 
... (truncated)
```

### `GET /api/apps/list-available`

**List All Available Apps** — List all available apps (including not installed).

**Response 200**: array[`AppInfo`]

### `POST /api/apps/install` 🔴

**Install App** — Install a new app by its info (background, returns job_id).

**Body** (required): application/json: `AppInfo`

**Response 200**: object[str → string]

### `POST /api/apps/remove/{app_name}` 🔴

**Remove App** — Remove an installed app by its name (background, returns job_id).

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `app_name` | path | yes | string |  |  |

**Response 200**: object[str → string]

### `GET /api/apps/job-status/{job_id}`

**Job Status** — Get status/logs for a job.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `job_id` | path | yes | string |  |  |

**Response 200**: `JobInfo`

### `POST /api/apps/start-app/{app_name}`

**Start App** — Start an app by its name.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `app_name` | path | yes | string |  |  |

**Response 200**: `AppStatus`

### `POST /api/apps/start-app/{app_name}/no-evict`

**Start App No Evict** — Start an app only when the managed robot slot can be acquired atomically.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `app_name` | path | yes | string |  |  |

**Response 200**: `AppStatus`

### `POST /api/apps/restart-current-app` 🔴

**Restart App** — Restart the currently running app.

**Response 200**: `AppStatus`

### `POST /api/apps/stop-current-app` 🔴

**Stop App** — Stop the currently running app.

**Response 200**: any

### `GET /api/apps/current-app-status`

**Current App Status** — Get the status of the currently running app, if any.

**Response 200**: `AppStatus` \| null

Example (`GET /api/apps/current-app-status`):

```json
null
```

### `GET /api/apps/startup-app`

**Get Startup App** — Get the app configured to auto-start on wake-up (null if unset).

**Response 200**: `StartupApp`

Example (`GET /api/apps/startup-app`):

```json
{
  "startup_app": null
}
```

### `PUT /api/apps/startup-app` 🔴

**Set Startup App** — Set (or clear, with null) the app that auto-starts on wake-up. Must already be installed (install via /apps/install first). Applied live, so no daemon restart is needed.

**Body** (required): application/json: `StartupApp`

**Response 200**: `StartupApp`

### `POST /api/apps/install-private-space` 🔴

**Install Private Space** — Install a private HuggingFace space. Requires HF token to be stored via /api/hf-auth/save-token first.

**Body** (required): application/json: `PrivateSpaceInstallRequest`

**Response 200**: object[str → string]

### `GET /api/apps/check-updates`

**Check App Updates** — Check all installed apps for available updates. Results are cached for 5 minutes unless force=True. This performs a 'slow' check with rate limiting to avoid overwhelming HuggingFace.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `force` | query | no | boolean | false |  |

**Response 200**: `AppUpdatesResponse`

### `POST /api/apps/update/{app_name}` 🔴

**Update App** — Update an installed app to the latest version. This reinstalls the app from HuggingFace, which downloads the latest version. Returns a job_id for tracking progress via WebSocket.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `app_name` | path | yes | string |  |  |

**Response 200**: object[str → string]

## Kinematics

### `GET /api/kinematics/info`

**Get Kinematics Info** — Get the current information of the kinematics.

**Response 200**: object

Example (`GET /api/kinematics/info`):

```json
{
  "info": {
    "engine": "AnalyticalKinematics",
    "collision check": false
  }
}
```

### `GET /api/kinematics/urdf`

**Get Urdf** — Get the URDF representation of the robot.

**Response 200**: object[str → string]

### `GET /api/kinematics/stl/{filename}`

**Get Stl File** — Get the path to an STL asset file.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `filename` | path | yes | string |  |  |

**Response 200**: any

## Hugging Face auth

### `POST /api/hf-auth/save-token` 🔴

**Save Token** — Save HuggingFace token after validation.

**Body** (required): application/json: `TokenRequest`

**Response 200**: `TokenResponse`

### `GET /api/hf-auth/status`

**Get Auth Status** — Check if user is authenticated with HuggingFace.

**Response 200**: object

Example (`GET /api/hf-auth/status`):

```json
{
  "is_logged_in": false,
  "username": null
}
```

### `GET /api/hf-auth/relay-status`

**Get Relay Status** — Get the central signaling relay connection status.

**Response 200**: object

### `DELETE /api/hf-auth/token` 🔴

**Delete Token** — Delete stored HuggingFace token.

**Response 200**: object[str → string]

### `POST /api/hf-auth/refresh-relay` 🔴

**Refresh Relay** — Force the central signaling relay to reconnect — see ``notify_force_reconnect``. Response shapes: - ``{"status": "requested", "token_available": bool}`` — a reconnect was kicked off. - ``{"status": "skipped", "token_available": bool, "reason": "relay_not_running" | "relay_unavailable"}`` — no reconnect happened. The mobile app's auto-heal loop must NOT wait for a state change in this case (it would hang forever). ``relay_unavailable`` covers the import failure (Lite-only build that ships no relay module); ``relay_not_running`` covers the module-present-but-no-instance case (daemon started without a token / pre-init / shutdown).

**Response 200**: object

### `GET /api/hf-auth/central-robot-status`

**Get Central Robot Status** — Proxy to the central signaling server's /api/robot-status endpoint. Uses the stored HF token server-side so the desktop frontend never sees the raw token. The frontend polls this to know whether any of the user's robots is currently held by a remote JS app, and if so which one (so it can show "In use by Hand Tracker" in the UI). Response shape: { "available": bool, "robots": [...], "reason": str? } `available` is false when: - no HF token stored (user not logged in) - central server is unreachable / returned an error Callers should treat `available: false` as "unknown, don't block".

**Response 200**: object

### `GET /api/hf-auth/oauth/configured`

**Is Oauth Configured** — Check if OAuth is configured.

**Response 200**: object

Example (`GET /api/hf-auth/oauth/configured`):

```json
{
  "configured": true
}
```

### `GET /api/hf-auth/oauth/start`

**Start Oauth** — Start a new OAuth authorization session. Returns the auth_url to redirect the user to HuggingFace. Args: request: The incoming HTTP request. use_localhost: When True, use localhost:8000 as the OAuth callback URL. Passed by the desktop app which proxies localhost:8000 to the robot.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `use_localhost` | query | no | boolean | false |  |

**Response 200**: object

### `GET /api/hf-auth/oauth/begin`

**Begin Oauth** — One-shot OAuth entry point for the mobile setup flow. Creates a session and 302-redirects the browser straight to Hugging Face's authorize page, so the phone only has to open this single URL. The existing /oauth/callback finishes the job (exchange → store token → start relay).

**Response 200**: any

### `GET /api/hf-auth/oauth/status/{session_id}`

**Get Oauth Status** — Poll for OAuth session status. The frontend polls this endpoint to check if the user has completed authorization.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `session_id` | path | yes | string |  |  |

**Response 200**: object

### `DELETE /api/hf-auth/oauth/session/{session_id}`

**Cancel Oauth Session** — Cancel an OAuth session.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `session_id` | path | yes | string |  |  |

**Response 200**: object[str → string]

### `POST /api/hf-auth/oauth/device/start`

**Start Device Oauth** — Start a device-code OAuth login. Returns a `user_code` and `verification_uri(_complete)` to show the user, plus a `session_id` to poll via `/oauth/device/status/{session_id}`.

**Response 200**: object

### `GET /api/hf-auth/oauth/device/status/{session_id}`

**Get Device Oauth Status** — Poll for device-code login completion. On the first poll that reports success, bring the central relay up (a token-less boot has no relay instance yet), mirroring the OAuth callback.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `session_id` | path | yes | string |  |  |

**Response 200**: object

### `DELETE /api/hf-auth/oauth/device/session/{session_id}`

**Cancel Device Oauth Session** — Cancel a pending device-code login session.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `session_id` | path | yes | string |  |  |

**Response 200**: object[str → string]

### `GET /api/hf-auth/oauth/callback`

**Oauth Callback** — Handle OAuth callback from HuggingFace. This is where HF redirects after user authorizes. Shows a success/error page that the user can close.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `code` | query | no | string \| null |  |  |
| `state` | query | no | string \| null |  |  |
| `error` | query | no | string \| null |  |  |
| `error_description` | query | no | string \| null |  |  |

**Response 200**: any

## Wi-Fi

### `GET /wifi/status`

**Get Wifi Status** — Get the current WiFi status.

**Response 200**: `WifiStatus`

Example (`GET /wifi/status`):

```json
{
  "mode": "wlan",
  "known_networks": [
    "Reachy Mini",
    "ADSAI Reachy Mini",
    "Caio",
    "guti",
    "iPhone 11 van Cas"
  ],
  "connected_network": "Reachy Mini"
}
```

### `GET /wifi/error`

**Get Last Wifi Error** — Get the last WiFi error.

**Response 200**: object[str → string \| null]

Example (`GET /wifi/error`):

```json
{
  "error": null
}
```

### `POST /wifi/reset_error` 🔴

**Reset Last Wifi Error** — Reset the last WiFi error.

**Response 200**: object[str → string]

### `POST /wifi/setup_hotspot` 🔴

**Setup Hotspot** — Set up a WiFi hotspot. It will create a new hotspot using nmcli if one does not already exist.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `ssid` | query | no | string | "reachy-mini-ap" |  |
| `password` | query | no | string | "reachy-mini" |  |

**Response 200**: any

### `POST /wifi/connect` 🔴

**Connect To Wifi Network** — Connect to a WiFi network. It will create a new connection using nmcli if the specified SSID is not already configured.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `ssid` | query | yes | string |  |  |
| `password` | query | yes | string |  |  |

**Response 200**: any

### `POST /wifi/scan_and_list`

**Scan Wifi** — Scan for available WiFi networks ordered by signal power.

**Response 200**: array[string]

### `POST /wifi/forget` 🔴

**Forget Wifi Network** — Forget a saved WiFi network. Falls back to Hotspot if forgetting the active network.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `ssid` | query | yes | string |  |  |

**Response 200**: any

### `POST /wifi/forget_all` 🔴

**Forget All Wifi Networks** — Forget all saved WiFi networks (except Hotspot). Falls back to Hotspot.

**Response 200**: any

### `GET /wifi/prov_key`

**Get Provisioning Key** — Return the robot's ephemeral X25519 public key for sealed provisioning. Public on purpose: a bare public key is useless to an attacker who does not also know the device PIN (which is mixed into the key derivation).

**Response 200**: object[str → string]

### `POST /wifi/connect_sealed` 🔴

**Connect To Wifi Network Sealed** — Connect to a WiFi network using an encrypted password. The plaintext password never crosses BLE or the local HTTP hop — it is AES-GCM-sealed by the phone and opened here. See the scheme comment above.

**Body** (required): application/json: `SealedConnect`

**Response 200**: any

## Update

### `GET /update/install-source`

**Install Source** — Get current install source: version, source type (pypi/git/editable), and git ref if applicable.

**Response 200**: object[str → string]

Example (`GET /update/install-source`):

```json
{
  "version": "1.10.0",
  "source": "pypi"
}
```

### `GET /update/available`

**Available** — Check if an update is available for Reachy Mini Wireless.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `pre_release` | query | no | boolean | false |  |

**Response 200**: object[str → object[str → object[str → boolean \| string]]]

### `POST /update/start` 🔴

**Start Update** — Start the update process for Reachy Mini Wireless version.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `pre_release` | query | no | boolean | false |  |

**Response 200**: object[str → string]

### `GET /update/validate-ref`

**Validate Ref** — Check if a git ref (tag/branch) exists on GitHub.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `git_ref` | query | yes | string |  |  |

**Response 200**: object[str → boolean \| string]

### `POST /update/start-from-ref` 🔴

**Start Update From Ref** — Start update from a specific git ref (tag or branch).

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `git_ref` | query | yes | string |  |  |

**Response 200**: object[str → string]

### `GET /update/info`

**Get Update Info** — Get the info of an update job.

| Param | In | Required | Type | Default | Description |
|---|---|---|---|---|---|
| `job_id` | query | yes | string |  |  |

**Response 200**: `JobInfo`

## Cache

### `POST /cache/clear-hf` 🔴

**Clear Huggingface Cache** — Clear HuggingFace cache directory.

**Response 200**: object[str → string]

### `POST /cache/reset-apps` 🔴

**Reset Apps** — Remove applications virtual environment directory.

**Response 200**: object[str → string]

## Web pages (redirects)

### `GET /`

**Dashboard** — Redirect to the desktop app download page.

**Response 200**: any

### `GET /settings`

**Settings** — Redirect to the desktop app download page.

**Response 200**: any

### `GET /logs`

**Logs Page** — Redirect to the desktop app download page.

**Response 200**: any

## Schemas

### `AppInfo`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |
| `source_kind` | `SourceKind` | yes |  |  |
| `description` | string | no | "" |  |
| `url` | string \| null | no |  |  |
| `extra` | object | no |  |  |

### `AppState`

Status of a running app.

Enum: `starting`, `running`, `done`, `stopping`, `error`

### `AppStatus`

Status of an app.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `info` | `AppInfo` | yes |  |  |
| `state` | `AppState` | yes |  |  |
| `error` | string \| null | no |  |  |

### `AppUpdateStatus`

Status of an app update check.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `app_name` | string | yes |  |  |
| `space_id` | string | yes |  |  |
| `installed_sha` | string | yes |  |  |
| `latest_sha` | string | yes |  |  |
| `update_available` | boolean | yes |  |  |
| `last_modified` | string \| null | no |  |  |

### `AppUpdatesResponse`

Response for list of app updates.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `apps_with_updates` | array[`AppUpdateStatus`] | yes |  |  |
| `apps_checked` | integer | yes |  |  |
| `apps_skipped` | integer | yes |  |  |

### `ApplyAudioConfigRequest`

Request body for ``POST /audio/config/apply``.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `config` | array[`AudioParamPair`] | yes |  |  |
| `verify` | boolean | no | true |  |

### `ApplyAudioConfigResponse`

Response body for ``POST /audio/config/apply``.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `applied` | boolean | yes |  |  |

### `AudioParamPair`

One ``(parameter_name, values)`` pair in an audio config payload.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |
| `values` | array[number] | yes |  |  |

### `Body_upload_sound_api_media_sounds_upload_post`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `file` | string | yes |  |  |

### `CameraSpecsResponse`

Full camera specifications as detected by the daemon.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |
| `available_resolutions` | array[`ResolutionInfo`] | yes |  |  |
| `default_resolution` | `ResolutionInfo` | yes |  |  |
| `K` | array[array[number]] | yes |  |  |
| `D` | array[number] | yes |  |  |

### `DaemonState`

Enum representing the state of the Reachy Mini daemon.

Enum: `not_initialized`, `starting`, `running`, `stopping`, `stopped`, `error`

### `DaemonStatus`

Status of the Reachy Mini daemon.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `type` | string | no | "daemon_status" |  |
| `robot_name` | string | yes |  |  |
| `state` | `DaemonState` | yes |  |  |
| `wireless_version` | boolean | yes |  |  |
| `desktop_app_daemon` | boolean | yes |  |  |
| `simulation_enabled` | boolean \| null | yes |  |  |
| `mockup_sim_enabled` | boolean \| null | yes |  |  |
| `no_media` | boolean | no | false |  |
| `media_released` | boolean | no | false |  |
| `camera_specs_name` | string | no | "" |  |
| `backend_status` | `RobotBackendStatus` \| `MujocoBackendStatus` \| `MockupSimBackendStatus` \| null | yes |  |  |
| `error` | string \| null | no |  |  |
| `wlan_ip` | string \| null | no |  |  |
| `version` | string \| null | no |  |  |
| `hardware_id` | string \| null | no |  |  |
| `face_target` | `FaceTarget` | no |  |  |

### `DoaSnapshot`

Sound Direction of Arrival reading (ReSpeaker mic array). ``angle`` is in radians: 0 = left, π/2 = front, π = right.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `angle` | number | yes |  |  |
| `speech_detected` | boolean | yes |  |  |

### `EnableTrackingRequest`

Request body for enabling daemon-side head tracking.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `weight` | number | no | 1.0 |  |

### `FaceTarget`

Latest daemon-side face target used for head tracking.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `detected` | boolean | no | false |  |
| `x` | number \| null | no |  |  |
| `y` | number \| null | no |  |  |
| `roll` | number \| null | no |  |  |
| `ts` | number \| null | no |  |  |

### `FullBodyTarget`

Represent the full body including the head pose and the joints for antennas.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `target_head_pose` | `XYZRPYPose` \| `Matrix4x4Pose` \| null | no |  |  |
| `target_antennas` | array[any] \| null | no |  |  |
| `target_body_yaw` | number \| null | no |  |  |
| `timestamp` | string \| null | no |  |  |

### `FullState`

Represent the full state of the robot including all joint positions and poses.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `control_mode` | `MotorControlMode` \| null | no |  |  |
| `head_pose` | `XYZRPYPose` \| `Matrix4x4Pose` \| null | no |  |  |
| `head_joints` | array[number] \| null | no |  |  |
| `body_yaw` | number \| null | no |  |  |
| `antennas_position` | array[number] \| null | no |  |  |
| `timestamp` | string \| null | no |  |  |
| `passive_joints` | array[number] \| null | no |  |  |
| `doa` | `DoaSnapshot` \| null | no |  |  |

### `GotoModelRequest`

Request model for the goto endpoint.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `head_pose` | `XYZRPYPose` \| `Matrix4x4Pose` \| null | no |  |  |
| `antennas` | array[any] \| null | no |  |  |
| `body_yaw` | number \| null | no |  |  |
| `duration` | number | yes |  |  |
| `interpolation` | `InterpolationTechnique` | no | "minjerk" |  |

### `HTTPValidationError`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `detail` | array[`ValidationError`] | no |  |  |

### `InterpolationTechnique`

Enumeration of interpolation techniques.

Enum: `linear`, `minjerk`, `ease_in_out`, `cartoon`

### `JobInfo`

Pydantic model for install job status.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `command` | string | yes |  |  |
| `status` | `JobStatus` | yes |  |  |
| `logs` | array[string] | yes |  |  |

### `JobStatus`

Enum for job status.

Enum: `pending`, `in_progress`, `done`, `failed`

### `Matrix4x4Pose`

Represent a 3D pose by its 4x4 transformation matrix (translation is expressed in meters).

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `m` | array[any] | yes |  |  |

### `MockupSimBackendStatus`

Status of the MockupSim backend.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `motor_control_mode` | `MotorControlMode` | yes |  |  |
| `error` | string \| null | no |  |  |

### `MotorControlMode`

Enum for motor control modes.

Enum: `enabled`, `disabled`, `gravity_compensation`

### `MotorStatus`

Represents the status of the motors. Exposes - mode: The current motor control mode (enabled, disabled, gravity_compensation).

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `mode` | `MotorControlMode` | yes |  |  |

### `MoveUUID`

Model representing a unique identifier for a move task.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `uuid` | string | yes |  |  |

### `MujocoBackendStatus`

Status of the Mujoco backend.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `motor_control_mode` | `MotorControlMode` | yes |  |  |
| `error` | string \| null | no |  |  |

### `PlaySoundRequest`

Request body for the play_sound endpoint.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `file` | string | yes |  |  |

### `PrivateSpaceInstallRequest`

Request model for installing a private HuggingFace space.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `space_id` | string | yes |  |  |

### `ReadAudioParameterResponse`

Response body for ``GET /audio/config/parameter/{name}``.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |
| `values` | array[number \| integer] | yes |  |  |

### `ResolutionInfo`

A single camera resolution entry.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |
| `width` | integer | yes |  |  |
| `height` | integer | yes |  |  |
| `fps` | integer | yes |  |  |
| `crop_factor` | number | yes |  |  |

### `RobotAppLockState`

Lock state enum. Values are stable strings suitable for serialization.

Enum: `free`, `local_app`, `remote_session`

### `RobotAppLockStatus`

Snapshot of the lock state, suitable for JSON serialization. Returned by :meth:`RobotAppLock.status` and by the ``GET /api/daemon/robot-app-lock-status`` endpoint.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `state` | `RobotAppLockState` | yes |  |  |
| `holder_name` | string \| null | no |  |  |

### `RobotBackendStatus`

Status of the Robot Backend.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `ready` | boolean | yes |  |  |
| `motor_control_mode` | `MotorControlMode` | yes |  |  |
| `last_alive` | number \| null | yes |  |  |
| `control_loop_stats` | object | yes |  |  |
| `error` | string \| null | no |  |  |

### `RobotNameRequest`

Body for setting the robot display name.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  |  |

### `RobotNameResponse`

The robot's current display name (``null`` only if none is resolvable).

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string \| null | no |  |  |

### `SealedConnect`

Sealed WiFi-connect payload (base64 fields, see scheme above).

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `ssid` | string | yes |  |  |
| `kid` | string | yes |  |  |
| `epk` | string | yes |  |  |
| `nonce` | string | yes |  |  |
| `ct` | string | yes |  |  |

### `SourceKind`

Kinds of app source.

Enum: `hf_space`, `dashboard_selection`, `local`, `installed`

### `StartupApp`

The app configured to auto-start on the robot's first wake-up.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `startup_app` | string \| null | no |  |  |

### `TestSoundResponse`

Response model for test sound operations.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `status` | string | yes |  |  |
| `message` | string | yes |  |  |

### `TokenRequest`

Request model for saving a HuggingFace token.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `token` | string | yes |  |  |

### `TokenResponse`

Response model for token operations.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `status` | string | yes |  |  |
| `username` | string \| null | no |  |  |
| `message` | string \| null | no |  |  |

### `ValidationError`

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `loc` | array[string \| integer] | yes |  |  |
| `msg` | string | yes |  |  |
| `type` | string | yes |  |  |

### `VolumeRequest`

Request model for setting volume.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `volume` | integer | yes |  | Volume level (0-100) |

### `VolumeResponse`

Response model for volume operations.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `volume` | integer | yes |  |  |
| `platform` | string | yes |  |  |
| `device` | string | yes |  |  |

### `WifiMode`

WiFi possible modes.

Enum: `hotspot`, `wlan`, `disconnected`, `busy`

### `WifiStatus`

WiFi status model.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `mode` | `WifiMode` | yes |  |  |
| `known_networks` | array[string] | yes |  |  |
| `connected_network` | string \| null | yes |  |  |

### `XYZRPYPose`

Represent a 3D pose using position (x, y, z) in meters and orientation (roll, pitch, yaw) angles in radians.

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `x` | number | no | 0.0 |  |
| `y` | number | no | 0.0 |  |
| `z` | number | no | 0.0 |  |
| `roll` | number | no | 0.0 |  |
| `pitch` | number | no | 0.0 |  |
| `yaw` | number | no | 0.0 |  |
